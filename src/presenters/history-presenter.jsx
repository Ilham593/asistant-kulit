export default class HistoryPresenter {
    // fetch daftar riwayat dari backend (stub)
    async fetchHistory(token) {
      // di real app, pakai fetch('/api/history', { headers:{ Authorization:`Bearer ${token}`} })
      return []; // stub: kosong
    }
  }
  